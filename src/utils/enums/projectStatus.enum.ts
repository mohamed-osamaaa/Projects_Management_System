export enum ProjectStatus {
    PENDING = 'pending',
    IN_PROGRESS = 'in_progress',
    UNDER_REVIEW = 'under_review',
    COMPLETED = 'completed',
    PAUSED = 'paused',
    CANCELED = 'canceled',
    EXPIRED = 'expired',
    REPUBLISHED = 'republished',
}
